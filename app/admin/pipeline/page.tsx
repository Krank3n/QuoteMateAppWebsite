'use client';
import ComingSoon from '../components/ComingSoon';

export default function PipelinePage() {
  return <ComingSoon title="Pipeline" hint="Kanban view of Lead → Trial → Active → Pro → Churned. Drag cards between columns to retag users. Coming next." />;
}
